spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/owz-zhou/hello-world
          searchPaths: hello-world
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
